/**
 * Enumeración que representa los posibles estados de un recurso o entidad en el sistema.
 */
export enum Status {
    /** Estado activo, indica que el recurso está habilitado y en funcionamiento. */
    active = 'active',

    /** Estado inactivo, indica que el recurso está deshabilitado o no disponible. */
    inactive = 'inactive'
}
