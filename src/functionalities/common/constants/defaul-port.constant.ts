/**
 * Puerto por defecto en el que se ejecutará el servidor.
 *
 * Esta constante define el puerto en el que la aplicación escuchará las solicitudes 
 * si no se especifica otro puerto en la configuración.
 *
 * @constant {number}
 */
export const DEFAULT_PORT: number = 3000;
